# Forge Assumptions

**Last updated:** 2026-03-18

- `settings.local.json` existed at commit time and was included
- CRLF line-ending normalization warnings on Windows are expected and non-blocking
- No `.gitattributes` was requested; left for a follow-up if needed
